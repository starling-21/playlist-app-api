version: "3"

services:
    django-server:
        build:
            context: .
            dockerfile: Dockerfile
        # ports:
        #     - 80:8000
        volumes:
            - ./app:/app
        # command: >
        #     sh -c "python manage.py runserver 0.0.0.0:8000"